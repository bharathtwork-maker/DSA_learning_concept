/*
store unique value but not in the sorted method 
here we cannot use lower_bound and upper_bound 
*/
#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> st;  //every other operation are the same as set and multiset
}
